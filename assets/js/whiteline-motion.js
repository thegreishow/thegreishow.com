(()=>{const nodes=document.querySelectorAll('main section,.metric-band');nodes.forEach(node=>node.classList.add('reveal'));if(!('IntersectionObserver'in window)){nodes.forEach(node=>node.classList.add('visible'));return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});nodes.forEach(node=>observer.observe(node))})();