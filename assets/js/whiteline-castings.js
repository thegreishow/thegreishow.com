(()=>{const cfg=window.WHITE_LINE_SUPABASE||{};if(!window.supabase?.createClient||!cfg.url||!cfg.anonKey)return;const db=window.supabase.createClient(cfg.url,cfg.anonKey),$=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let rows=[];async function load(){const {data,error}=await db.from('casting_calls').select('id,title,slug,category,location,event_date,application_deadline,description,requirements,compensation,status').eq('status','open').order('application_deadline',{ascending:true});if(error){$('casting-grid').innerHTML='<div class="empty">Could not load casting calls.</div>';return}rows=data||[];draw()}function draw(){const q=$('casting-search').value.toLowerCase().trim(),list=rows.filter(x=>[x.title,x.category,x.location,x.description,x.requirements,x.compensation].join(' ').toLowerCase().includes(q));$('casting-count').textContent=`${list.length} open call${list.length===1?'':'s'}`;$('casting-grid').innerHTML=list.length?list.map(x=>`<article class="card"><div class="card-body"><span class="pill">Open</span><h3>${esc(x.title)}</h3><div class="meta">${esc(x.category||'Casting')} · ${esc(x.location||'Location TBA')}</div>${x.event_date?`<p class="meta">Event: ${new Date(x.event_date+'T00:00:00').toLocaleDateString()}</p>`:''}${x.application_deadline?`<p class="meta">Deadline: ${new Date(x.application_deadline).toLocaleString()}</p>`:''}<p>${esc(x.description||'')}</p>${x.requirements?`<p class="meta">Requirements: ${esc(x.requirements)}</p>`:''}${x.compensation?`<strong>${esc(x.compensation)}</strong>`:''}<div class="actions"><a class="button primary" href="whiteline-apply.html?casting=${encodeURIComponent(x.id)}&title=${encodeURIComponent(x.title)}">Apply now</a></div></div></article>`).join(''):'<div class="empty">No public casting calls are open right now. Check back soon or create a profile to join the roster.</div>'}$('casting-search').oninput=draw;load()})();