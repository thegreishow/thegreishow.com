# Optional absolute origin for production-equivalent links and assets.
# Leave blank during branch-preview development to use the current preview origin.
VITE_SITE_ORIGIN=

# Optional JSON POST endpoint for newsletter submissions.
# When blank, the React lab validates the form without sending data.
VITE_NEWSLETTER_ENDPOINT=
